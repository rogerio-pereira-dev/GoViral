name: lint-autofix

on:
  push:
    branches-ignore:
      - main

permissions:
  contents: write

jobs:
  autofix:
    uses: ./.github/workflows/_lint-reusable.yml
    with:
      mode: fix
    secrets: inherit
